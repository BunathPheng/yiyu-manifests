---
# Source: helm for kubernetes/templates/ingress.yaml
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: yiyu                  # Ingress name
  labels:
    app: yiyu
spec:
  ingressClassName: nginx   # Optional ingressClass
  rules:
    - host: "yiyu.smartops.space"
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: yiyu               # Links to Service name dynamically
                port:
                  number: 8080  # Links to service port dynamically
